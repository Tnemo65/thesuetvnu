table_ref: tlc_trips
static_threshold: 0.05
expectation_rules:
  - feature: null_ratio__fare_amount
    op: <=
    threshold: 0.02
  - feature: null_ratio__trip_distance
    op: <=
    threshold: 0.02
  - feature: range_violation_ratio__trip_distance
    op: <=
    threshold: 0.01
  - feature: invalid_fk_ratio__PULocationID
    op: <=
    threshold: 0.00
